# План развития проекта

Документ описывает план развития проекта **ВкусноТут**.

## Общий подход

Проект развивается поэтапно.

Основной принцип:

```txt
Сначала рабочий MVP, потом расширение.
```

Архитектура проектируется с учётом будущих возможностей, но реализация выполняется постепенно.

## Этап 1. Базовая настройка проекта

Статус: частично выполнено.

Задачи:

- создать корневой проект;
- создать GitHub-репозиторий;
- создать backend на NestJS;
- настроить запуск backend из корня;
- подключить `.env`;
- настроить базовые инструменты разработки;
- выбрать базу данных;
- выбрать ORM;
- подготовить документацию.

Проделано:

- создан backend на NestJS;
- настроена базовая конфигурация приложения;
- подключён `ConfigModule`;
- подготовлены `.env` / `.env.example`;
- выбрана PostgreSQL;
- выбрана TypeORM;
- подготовлена документация проекта;
- зафиксирован принцип: проектируем шире, реализуем уже.

## Этап 2. База данных и ORM

Статус: в процессе.

Задачи:

- подключить PostgreSQL;
- подключить TypeORM;
- настроить database config;
- подключить TypeORM в `AppModule`;
- проверить подключение backend к базе данных;
- настроить DataSource;
- настроить миграции;
- описать первые entity;
- создать первую миграцию;
- проверить создание таблиц.

Проделано:

- переустановлен PostgreSQL для чистой локальной разработки;
- выбрана версия PostgreSQL 16;
- создана локальная база данных `recipes_app_dev`;
- pgAdmin 4 оставлен как инструмент просмотра базы;
- зафиксировано, что pgAdmin не нужно запускать при каждой разработке;
- добавлены env-переменные для PostgreSQL;
- создана структура `src/config/database`;
- создан `databaseConfig`;
- создан тип `DatabaseConfig`;
- созданы утилиты чтения env-переменных;
- создан `getTypeOrmConfig`;
- `databaseConfig` добавлен в `configLoaders`;
- настроены публичные экспорты config-разделов;
- принято решение использовать `TypeOrmModule.forRootAsync`;
- принято решение использовать `synchronize: false`.

Ближайшие задачи:

- подключить `TypeOrmModule` в `AppModule`;
- запустить backend и проверить подключение к PostgreSQL;
- исправить возможные ошибки подключения;
- настроить TypeORM DataSource для миграций;
- добавить команды для миграций;
- создать первые entity;
- создать первую миграцию.

Планируемые сущности:

- User;
- Recipe;
- Category;
- Tag;
- Ingredient;
- Unit;
- IngredientUnitConversion;
- RecipeIngredient;
- RecipeStep;
- FavoriteRecipe.

## Этап 3. Seed-данные

Статус: не начато.

Задачи:

- добавить стартовые категории;
- добавить стартовые теги;
- добавить единицы измерения;
- добавить базовые ингредиенты;
- добавить тестовые рецепты.

## Этап 4. Первый API рецептов

Статус: не начато.

Задачи:

- получить список опубликованных рецептов;
- получить рецепт по slug;
- добавить фильтрацию по категории;
- добавить фильтрацию по тегам;
- добавить базовый поиск.

## Этап 5. Frontend

Статус: не начато.

Задачи:

- создать frontend-приложение на Next.js;
- настроить структуру frontend;
- подключить API;
- реализовать страницу списка рецептов;
- реализовать страницу рецепта;
- реализовать фильтры по категориям и тегам.

## Этап 6. Авторизация

Статус: не начато.

Задачи:

- регистрация;
- логин;
- JWT;
- refresh token;
- профиль пользователя;
- подтверждение email.

В будущем:

- вход через Яндекс;
- вход через ВК;
- вход через Google.

## Этап 7. Избранное

Статус: не начато.

Задачи:

- добавить рецепт в избранное;
- удалить рецепт из избранного;
- получить список избранных рецептов пользователя;
- отображать состояние избранного на frontend.

## Этап 8. Пользовательские рецепты

Статус: не начато.

Задачи:

- создание черновика рецепта;
- редактирование своего рецепта;
- добавление ингредиентов;
- добавление шагов приготовления;
- отправка рецепта на модерацию;
- просмотр своих рецептов.

## Этап 9. Модерация

Статус: не начато.

Задачи:

- список рецептов на модерации;
- одобрение рецепта;
- отклонение рецепта;
- причина отклонения;
- статусы рецепта.

## Этап 10. Админка

Статус: не начато.

Задачи:

- управление рецептами;
- управление пользователями;
- управление ролями;
- управление категориями;
- управление тегами;
- управление ингредиентами;
- управление единицами измерения.

## Этап 11. Расчёт КБЖУ

Статус: не начато.

Задачи:

- хранить КБЖУ ингредиентов на 100 г;
- хранить единицы измерения;
- хранить переводы единиц в граммы;
- считать КБЖУ всего блюда;
- считать КБЖУ на 100 г готового блюда;
- поддержать ручной и автоматический режим расчёта.

## Этап 12. Изображения

Статус: не начато.

Задачи:

- загрузка главного изображения рецепта;
- пользовательские фото;
- фото шагов приготовления;
- модерация изображений.

## Что не входит в первый MVP

На первом этапе не реализуются:

- сложная админка;
- OAuth;
- email-подтверждение;
- комментарии;
- рейтинги;
- подписки;
- рекомендации;
- планировщик питания;
- список покупок;
- сложные подборки;
- продвинутая модерация;
- полноценный калькулятор КБЖУ.

Эти возможности заложены в архитектуру, но будут добавляться позже.

<!-- TYPEORM_DATASOURCE_STAGE_START -->
## Обновление по этапу 2: TypeORM DataSource и миграции

Статус этапа **"База данных и ORM"**: в процессе.

Дополнительно проделано:

- создан TypeORM DataSource для CLI;
- установлена зависимость `dotenv` в backend;
- настроено чтение `.env` и `.env.local` для миграций;
- добавлены команды миграций в `backend/package.json`;
- исправлена проблема с alias `@config` в `data-source.ts`;
- команда `migration:show` успешно выполняется;
- подтверждено, что TypeORM CLI готов к дальнейшей работе с миграциями.

Ближайшие задачи:

- создать первую entity;
- сгенерировать первую миграцию;
- применить миграцию;
- проверить таблицы в базе `recipes_app_dev`;
- после этого перейти к полноценному описанию базовых сущностей проекта.
<!-- TYPEORM_DATASOURCE_STAGE_END -->

<!-- FIRST_CATEGORY_MIGRATION_STAGE_START -->
## Обновление этапа 2: база данных и ORM

Статус этапа: в процессе.

Выполнено:

- настроено подключение NestJS к PostgreSQL через TypeORM;
- создан отдельный TypeORM DataSource для CLI и миграций;
- настроена папка `backend/src/database/migrations`;
- создана первая бизнес-сущность `Category`;
- сущность размещена по модульному принципу в `backend/src/modules/categories/entities`;
- сгенерирована первая миграция `CreateCategoriesTable`;
- миграция успешно применена к локальной базе `recipes_app_dev`;
- в PostgreSQL создана таблица `categories`;
- подтверждён рабочий цикл `Entity → Migration → PostgreSQL`.

Следующие шаги:

- создать `CategoriesModule`;
- подключить `TypeOrmModule.forFeature([Category])`;
- добавить `CategoriesService`;
- добавить `CategoriesController`;
- реализовать получение списка категорий через API;
- позже добавить seed-данные для стартовых категорий.
<!-- FIRST_CATEGORY_MIGRATION_STAGE_END -->

<!-- CATEGORIES_API_SWAGGER_STAGE_START -->
## Обновление этапа 2: модуль категорий и стартовое API

Статус этапа: в процессе.

Выполнено после первой миграции:

- создан `CategoriesModule`;
- создан `CategoriesService`;
- создан `CategoriesController`;
- `CategoriesModule` подключён в `AppModule`;
- реализована ручка получения активных категорий;
- добавлен seed-скрипт для стартового наполнения категорий;
- подключён Swagger/OpenAPI для документации API;
- стандартные `AppController` и `AppService` удалены как лишний стартовый код;
- подтверждён первый backend-флоу от базы данных до HTTP API.

Текущий рабочий backend-флоу:

```txt
PostgreSQL → TypeORM → Service → Controller → API
```

Доступная ручка:

```txt
GET /{apiPrefix}/categories
```

Если `apiPrefix=api`:

```txt
GET /api/categories
```

Swagger UI:

```txt
/{apiPrefix}/swagger
```

Если `apiPrefix=api`:

```txt
/api/swagger
```

Следующие шаги:

- проверить и зафиксировать API категорий в документации;
- добавить документацию по seed-данным категорий;
- перейти к следующему справочнику: `tags` или `units`;
- после справочников перейти к проектированию ингредиентов и рецептов.
<!-- CATEGORIES_API_SWAGGER_STAGE_END -->

<!-- TAGS_ROADMAP_STAGE_START -->
## Обновление roadmap: справочник тегов

Этап справочника тегов выполнен.

Сделано:

- добавлена сущность `Tag`;
- создана таблица `tags`;
- добавлен уникальный индекс для `slug`;
- создан `TagsModule`;
- создан `TagsService`;
- создан `TagsController`;
- создан `TagResponseDto`;
- добавлена публичная ручка `GET /{apiPrefix}/tags`;
- при `apiPrefix=api` ручка доступна как `GET /api/tags`;
- ручка тегов описана в Swagger;
- seed-скрипт расширен стартовыми тегами;
- справочники `categories` и `tags` теперь готовы для будущего каталога рецептов.

Текущий статус backend:

- базовая конфигурация NestJS выполнена;
- конфигурация env выполнена;
- PostgreSQL подключён;
- TypeORM подключён;
- DataSource для TypeORM CLI настроен;
- миграции работают;
- Swagger подключён;
- справочник категорий реализован;
- справочник тегов реализован.

Следующие рекомендуемые этапы:

1. Добавить справочник единиц измерения.
2. Добавить справочник ингредиентов.
3. Спроектировать сущность рецепта.
4. Добавить связи рецепта с категорией, тегами и ингредиентами.
5. Реализовать публичные ручки каталога рецептов:
   - `GET /{apiPrefix}/recipes`;
   - `GET /{apiPrefix}/recipes/:slug`.
6. После появления публичного каталога перейти к первому frontend-слою.
<!-- TAGS_ROADMAP_STAGE_END -->

<!-- UNITS_ROADMAP_STAGE_START -->
## Обновление roadmap: справочник единиц измерения

Завершён этап справочника единиц измерения.

Выполнено:

- создана сущность `Unit`;
- создан enum `UnitType`;
- создан `UnitsModule`;
- создан `UnitsService`;
- создан `UnitsController`;
- создан `UnitResponseDto`;
- добавлена публичная ручка `GET /{apiPrefix}/units`;
- при `apiPrefix=api` ручка доступна как `GET /api/units`;
- ручка описана в Swagger/OpenAPI;
- создана таблица `units`;
- добавлен уникальный индекс по `slug`;
- seed-скрипт расширен стартовыми единицами измерения;
- проверена работа справочника через API.

Текущий backend-слой справочников включает:

- `categories`;
- `tags`;
- `units`.

Следующий рекомендуемый этап:

```txt
справочник ингредиентов
```

После ингредиентов можно переходить к таблице пересчётов `ingredient_unit_conversions`, а затем к проектированию основной сущности `Recipe` и состава рецепта.
<!-- UNITS_ROADMAP_STAGE_END -->
