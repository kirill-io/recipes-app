# Категории и теги

Документ описывает подход к категориям и тегам в проекте **ВкусноТут**.

## Основная идея

Категории и теги решают разные задачи.

```txt
Категория = основной раздел сайта
Тег = дополнительный признак рецепта
```

Категория должна отвечать на вопрос:

```txt
Что это за тип блюда?
```

Тег должен отвечать на вопросы:

```txt
Как это готовится?
Чем это удобно?
Для какого случая подходит?
Какая особенность у рецепта?
```

## Почему не стоит копировать структуру книги

В исходных рецептах могут быть авторские разделы:

- минимум усилий;
- блюда в контейнерах;
- блюда в рукаве;
- фастфуд для похудения;
- во время переездов;
- новогодний стол.

Для книги такая структура нормальная, но для сайта удобнее использовать классическую навигацию.

Авторские разделы лучше превратить в теги или подборки.

## Планируемые категории

На первом этапе:

- Завтраки
- Супы
- Основные блюда
- Гарниры
- Перекусы
- Салаты
- Десерты
- Выпечка
- Соусы
- Напитки
- Праздничные блюда

## Правила категорий

1. У рецепта должна быть одна основная категория.
2. Категории не должны дублировать теги.
3. Категории должны быть понятны пользователю без контекста.
4. Категорий не должно быть слишком много.
5. Категории используются в основной навигации сайта.

## Планируемые теги

### Особенности

- быстро
- много белка
- низкокалорийное
- сытное
- простое
- для заготовки
- без готовки

### Способ приготовления

- запекаем
- жарим
- варим
- тушим
- блендерим

### Оборудование

- духовка
- сковорода
- аэрогриль
- микроволновка
- блендер
- мультиварка

### Приём пищи

- завтрак
- обед
- ужин
- перекус

### Повод

- праздничное
- на каждый день
- в дорогу
- на работу

## Типы тегов

В базе можно хранить тип тега.

Планируемые типы:

- `FEATURE`
- `METHOD`
- `EQUIPMENT`
- `MEAL`
- `DIET`
- `OCCASION`
- `SOURCE`

## Пример

Рецепт:

```txt
Паста с тунцом
```

Категория:

```txt
Основные блюда
```

Теги:

```txt
быстро
много белка
обед
ужин
без духовки
```

## Подборки

Подборки можно добавить позже.

Примеры будущих подборок:

- Быстрые завтраки
- Рецепты с творогом
- Блюда в духовке
- ПП-десерты
- Праздничные рецепты
- Рецепты без готовки

На первом этапе подборки можно не реализовывать.

<!-- CATEGORIES_API_SWAGGER_STAGE_START -->
## Обновление: реализация категорий

Категории — это основные разделы каталога рецептов.

На текущем этапе категории реализованы как отдельная таблица `categories` и доступны через backend API.

Категории используются для основного разделения рецептов по типу блюда.

Примеры стартовых категорий:

- Завтраки;
- Салаты;
- Супы;
- Основные блюда;
- Десерты;
- Перекусы;
- Напитки.

Категории имеют уникальный `slug`, который в будущем может использоваться в URL страниц каталога.

Пример:

```txt
/categories/breakfasts
/categories/salads
```

Backend API для получения активных категорий:

```txt
GET /{apiPrefix}/categories
```

Если `apiPrefix=api`:

```txt
GET /api/categories
```

Теги будут использоваться позже как дополнительные признаки рецепта: быстро, ПП, без сахара, вегетарианское, острое и другие.

Категория отвечает на вопрос:

```txt
Что это за тип блюда?
```

Тег отвечает на вопрос:

```txt
Какими дополнительными признаками обладает рецепт?
```
<!-- CATEGORIES_API_SWAGGER_STAGE_END -->

<!-- TAGS_PRODUCT_STAGE_START -->
## Обновление: стартовый справочник тегов

После категорий добавлен справочник тегов.

Категории и теги выполняют разные задачи.

Категории отвечают на вопрос:

```txt
К какому основному разделу относится рецепт?
```

Примеры категорий:

- завтраки;
- салаты;
- супы;
- основные блюда;
- десерты.

Теги отвечают на дополнительные вопросы:

```txt
Какой это рецепт?
Из чего он?
Как он готовится?
Для какой ситуации подходит?
Есть ли у него диетические особенности?
```

Примеры тегов:

- ПП;
- Быстро;
- Просто;
- Без сахара;
- Низкокалорийное;
- Высокобелковое;
- С курицей;
- С рыбой;
- С творогом;
- Овощное;
- На сковороде;
- В духовке;
- Без выпечки;
- На каждый день;
- Праздничное.

Для MVP принята модель:

- у рецепта будет одна основная категория;
- у рецепта может быть несколько тегов.

Такой подход позволит в будущем строить фильтрацию каталога:

```txt
/api/recipes?category=salads
/api/recipes?tag=bez-sahara
/api/recipes?tag=bystro
```

На текущем этапе связь `Recipe` ↔ `Tag` ещё не создаётся, потому что сущность рецепта ещё не реализована. Связь будет добавлена позже отдельной миграцией при создании рецептов.
<!-- TAGS_PRODUCT_STAGE_END -->

<!-- RECIPES_TAGS_PRODUCT_STAGE_START -->
## Использование тегов в рецептах

После добавления справочника тегов реализована связь рецептов с тегами через таблицу:

```txt
recipe_tags
```

Тег остаётся отдельным справочником, а `recipe_tags` хранит только связь конкретного рецепта с конкретным тегом.

Это позволяет:

- показывать теги на карточке рецепта;
- показывать теги на детальной странице рецепта;
- в будущем фильтровать каталог рецептов по тегам;
- расширять набор тегов без изменения структуры рецепта.

В публичных ответах рецепта тег отдаётся в компактном виде:

- `id`;
- `name`;
- `slug`.

Неактивные теги не попадают в массив `tags` у рецепта.
<!-- RECIPES_TAGS_PRODUCT_STAGE_END -->

<!-- RECIPES_FILTERS_SEARCH_PRODUCT_STAGE_START -->
## Использование категорий и тегов в фильтрации рецептов

Категории и теги теперь используются не только как справочники, но и как публичные фильтры каталога рецептов.

Категория остаётся основным разделом рецепта.

Через query-параметр `category` можно получить рецепты конкретного раздела:

```txt
GET /{apiPrefix}/recipes?category=breakfasts
GET /{apiPrefix}/recipes?category=main-dishes
```

Если `apiPrefix=api`, фактические примеры:

```txt
GET /api/recipes?category=breakfasts
GET /api/recipes?category=main-dishes
```

Теги используются как дополнительные признаки рецепта.

Через query-параметр `tag` можно получить рецепты с конкретным тегом:

```txt
GET /{apiPrefix}/recipes?tag=pp
GET /{apiPrefix}/recipes?tag=bystro
GET /{apiPrefix}/recipes?tag=s-kuricey
```

Фильтры можно комбинировать:

```txt
GET /{apiPrefix}/recipes?category=main-dishes&tag=pp
```

Для MVP это даёт базовую механику каталога:

- разделы по категориям;
- быстрые фильтры по тегам;
- поиск по рецептам;
- фильтр по сложности.

Такая структура подходит для первого frontend MVP: главная страница или каталог рецептов сможет получать категории, теги и список рецептов через публичные API.
<!-- RECIPES_FILTERS_SEARCH_PRODUCT_STAGE_END -->
